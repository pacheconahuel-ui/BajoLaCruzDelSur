# Copy this file to .env and fill in the values

# Optional — without this, the game works but partidas don't survive server restarts
# Local PostgreSQL example:
DATABASE_URL=postgresql://postgres:password@localhost:5432/7wonders

# Railway/Render example (they give you this URL automatically):
# DATABASE_URL=postgresql://user:pass@host:5432/dbname
